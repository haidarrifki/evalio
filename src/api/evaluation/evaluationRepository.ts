// import type { Evaluation } from './evaluationModel';

export class EvaluationRepository {
  // async createEvaluation(evaluation: Evaluation) {
  //   const evaluation = await db.insert(evaluation).values(evaluation);
  //   return evaluation;
  // }
}